/** 
 /** @typedef {import('./index').BeginCallback} BeginCallback */

/** @typedef {import('./index').DrawCallback} DrawCallback */

/** @typedef {import('./index').UpdateCallback} UpdateCallback */

/** @typedef {import('./index').EndCallback} EndCallback */

/** 
 * @typedef {Object} LoopState
 * @property {boolean} isVisible
 * @property {number} timestamp
 * @property {number} lastTimestamp 
 * @property {number} lastAbsence
 * @property {boolean} checkAway
 * @property {number} frame
 * @property {number} tick
 * @property {boolean} isRunning
 * @property {number} fps
 * @property {number} lastInterp
 * @property {number} lastDelta
 * @property {boolean} panic
 */

export const types = {}